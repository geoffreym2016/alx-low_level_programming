#include "main.h"

/**
 * strtow - splits a string into words
 *
 * @str: string
 *
 * Return: char**, array or strings
*/
int **strtow(char *str)
{
	int **array;

	if (str == NULL || str[0] == '\0')
		return (NULL);

	return (array);
}
